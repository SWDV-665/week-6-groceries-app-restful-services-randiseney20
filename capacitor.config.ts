import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Week3Groceries',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
